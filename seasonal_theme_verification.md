# 季節背景與手繪基金圖示驗證

## 實作範圍

本輪加入四組可選背景：春日、夏日、秋日與冬夜。選項存於 `investment-dashboard-season`，應用程式會在 React 首次渲染前還原 `html.season-*` 類別；深色模式也各自有夜間版本，不會覆寫既有 `investment-dashboard-theme` 偏好。

基金卡依基金類型使用原創的柔和手繪水彩資產：國內基金為晨光茶園與新芽，國際基金為月光羅盤與紙船。兩個檔案皆無文字、人物角色、商標或特定作品元素，採受控儲存 URL 載入，能隨專案生命週期保留。

## 互動與可讀性

| 項目 | 驗證結果 |
| --- | --- |
| 主題按鈕 | 以純 CSS 製作太陽與月亮圖案，點擊時使用 transform／opacity 平滑交替；使用者偏好減弱動態時停用轉場。 |
| 季節選擇 | 頂列提供「季節」選單，選擇後立即套用並保存於瀏覽器。 |
| 基金卡插畫 | 圖示位於卡片標題右側、與自選按鈕成組，不壓縮基金名稱、淨值、走勢與報酬欄位。 |
| 1280px | 季節選擇、主題按鈕、更新按鈕在同一頂列內可辨識；國內基金頁的搜尋、表格與卡片版面維持可讀。 |
| 375px | 側欄隱藏；季節控制壓縮為圖示與選值，插畫徽章縮為 36px，基金卡仍維持單欄閱讀。 |

## 自動化檢查

`pnpm exec tsc --noEmit` 通過；完整 Vitest 為 **31** 個測試檔、**115** 個案例通過。`seasonTheme.test.ts` 覆蓋合法選值、舊背景類別清除與本機偏好還原；`SeasonThemePicker.test.tsx` 另驗證選取冬夜後會同步保存 `localStorage` 並更新根元素類別。
