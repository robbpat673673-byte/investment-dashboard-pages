# 原物料與深色模式驗證紀錄

## 開發預覽：原物料側欄

左側原「全球指數」位置已改為「原物料」，並成功讀取五項 Yahoo Finance 公開期貨報價：黃金（GC=F）、WTI 原油（CL=F）、布蘭特原油（BZ=F）、銅（HG=F）與天然氣（NG=F）。原物料會顯示報價日期、最新價格及漲跌幅；原本的全球市場可拖曳卡片和表格維持原有指數內容，避免改變既有全球市場功能。

## 開發預覽：深色模式

點擊頂端「深色」按鈕後，首頁側欄、行情卡片、表格與基金資料皆切換為高對比深色配色，並保留綠漲、紅跌資料視覺。重新載入後按鈕顯示「淺色」，證明偏好已保存於此瀏覽器的 localStorage 並套用至頁面。

## 響應式檢查

桌面版已實測呈現五項原物料與深色高對比介面；375px 行動版依既有設計隱藏左側摘要欄，頂端深色模式按鈕與五個頁籤仍可見，不造成頁面橫向溢出。

## 正式公開網站：桌面版

正式網站在新版資產同步後已顯示五項原物料報價。點擊「深色」後，按鈕改為「淺色」，側欄、卡片與表格同時切換為高對比深色配色；重新載入正式網址後，按鈕仍為「淺色」，證明深色偏好已保存在此瀏覽器。

## 375px 行動版

以與正式發布版本相同的 375px 預覽確認：頂端深色模式按鈕可見、五個頁籤可橫向瀏覽，且主內容沒有橫向溢出。依既有行動版設計，原物料摘要側欄會收合隱藏，避免在窄螢幕壓縮行情內容；使用者可在桌面版查看原物料摘要。

正式公開網域已載入包含「切換為深色模式」的指紋化新版資產，並於桌面版實測切換、重載與 localStorage 偏好保存；相同發布版本在 375px 響應式預覽中保有深色按鈕與五頁籤，且沒有水平溢出。行動版延續既有設計隱藏側欄，避免五項原物料使主內容寬度超出視窗。

## 375px 正式公開網址實測（待修正）

正式公開網址於 375px 裝置模擬下可確認深色按鈕、五個頁籤與主內容寬度均位於 375px 視窗內，`scrollWidth` 為 375。切換後 `localStorage` 已寫入 `investment-dashboard-theme=dark`，按鈕也改為「淺色」；但重新載入後，按鈕保有「淺色」狀態而頁面根節點尚未恢復 `dark` 類別，造成視覺配色仍為淺色。此不一致已列為回歸修正項，不能視為行動版持久化驗證完成。

## 375px 正式公開網址回歸驗證（已修正）

已確認根層 `ThemeProvider` 在不可切換模式下仍會覆寫 `html.dark`，因此改為僅在 Context 主題切換啟用時才修改文件根節點；同時在 React 掛載前同步 `investment-dashboard-theme` 偏好。最新公開資產於全新 375px Chromium 工作階段的量測結果如下：切換前為 `dark=false`、`storedTheme=null`、按鈕「深色」；切換後為 `dark=true`、`storedTheme=dark`、按鈕「淺色」；重新載入後仍為 `dark=true`、`storedTheme=dark`、按鈕「淺色」。五個頁籤皆可識別，且 `window.innerWidth` 與 `document.documentElement.scrollWidth` 均為 375，未產生橫向溢出。最終截圖亦確認全頁深色配色已正確呈現。
