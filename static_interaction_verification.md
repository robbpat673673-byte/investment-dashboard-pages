# 公開靜態版互動功能驗證

驗證時間：2026-08-23（台北時間）

## 範圍

本輪新增的公開靜態版功能包括：重新讀取已發布資料、管理者 GitHub Actions 手動更新指引、基金與市場 CSV 匯出，以及市場／基金走勢圖的精確日期與數值提示。

## 桌面預覽結果

以靜態 Vite 預覽載入實際 `dashboard.json` 後，首頁能顯示「重新讀取最新資料」按鈕、資料快照時間、管理者更新展開指引、1M／3M／6M／1Y 區間控制、匯出市場 CSV 與匯出篩選基金 CSV 按鈕。八張市場卡與基金卡皆顯示圖表操作提示；游標移入或以鍵盤左右鍵操作時，介面會顯示取樣點的日期與精確數值。

實機以第一張市場卡的圖表取得焦點後送出左方向鍵，提示成功顯示 `2026-08-18` 與價格 `53,343.3984`，確認鍵盤操作能揭露取樣歷史點。

預覽快照的 `generatedAt` 為 `2026-08-22T12:45:08.615Z`，市場資料截至 `2026-08-21`。重新讀取功能只以無快取請求取得既有的公開 JSON，不會向外部行情來源重新抓取；真正資料更新需由具儲存庫寫入權限的管理者在 GitHub Actions 執行「更新並發布靜態資料」。

## 自動化驗證

完整 Vitest：35 個測試檔、131 項測試通過。TypeScript `tsc --noEmit` 通過；以 `VITE_BASE_PATH=/investment-dashboard-pages/ pnpm run static:build` 進行的靜態建置通過。
