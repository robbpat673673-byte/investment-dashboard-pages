# 深色模式與溫暖手繪面板驗證

## 視覺設計

本輪以自然紙張、森林綠與暖陽金色建立溫暖手繪的動畫工作室感，而未使用任何外部圖像或特定角色、商標。淺色模式採暖米白背景、柔和紙質面板、圓潤不規則卡片角與植物色系；深色模式採夜間林地色票，保留足夠的文字、資料漲跌與操作元件對比。

## 本機偏好

`ThemeToggle` 持續以 `investment-dashboard-theme` 儲存 `light`／`dark`，而 `main.tsx` 會在 React 首次渲染前還原選擇並更新 `html.dark`。本輪新增的單元測試另確認切換回淺色時會移除該類別。

## 畫面檢查

| 視角 | 結果 |
| --- | --- |
| 1280px 桌面 | 顯示暖色紙張背景、淺綠資料刷新面板、圓角分頁、橫向行情卡與資料表；漲跌色與更新時間仍可辨識。 |
| 375px 行動版 | 側欄依既有規則隱藏；主題切換與更新按鈕保留在頂列；刷新面板、區間選擇與市場卡改為可讀的窄螢幕配置，未見橫向文字溢出。 |
| 深色模式 | CSS token 已改為夜間森林色票，並將卡片、輸入、篩選、觀測站與詳細頁背景同步調整，避免淺色元件殘留。 |

## 測試狀態

`pnpm exec tsc --noEmit` 成功；完整 Vitest 為 29 個測試檔、111 個案例通過。Netlify 前端 production build 在 sandbox 記憶體超過 80% 時遭終止（exit 143），與 TypeScript 或 Vitest 錯誤無關；已停止額外預覽服務並重啟開發服務，待釋放更多記憶體後再重試建置。

