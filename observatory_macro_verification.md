# 觀測站總經擴充驗證紀錄

## 目前開發預覽觀察

兩次 1280×900 預覽均確認第六頁籤「觀測站」已在「財經即時新聞」右側正確顯示；但首頁資料區仍顯示「正在讀取公開資料庫」與「等待每日行情資料更新」，尚未取得 dashboard payload，因此尚不能宣稱總經行情、摘要生成或歷史查詢已完成畫面層驗證。

下一步應先檢查開發預覽的 network／browserConsole 與 server log，確認 dashboard query 未因新增行情或摘要 schema 契約而失敗；修正並重新載入資料後，再擷取桌面與 375px 觀測站畫面。

## API 與行動版檢查

本機 `dashboard.get` 回應包含 `TWD=X`、`DX-Y.NYB`、`^TNX` 與 `^TYX`；每日摘要生成端點已成功回傳「今日事實／市場觀察／限制與風險」，摘要歷史端點亦已回讀同一筆摘要。375px 預覽確認頁籤列可水平瀏覽；因截圖工具每次以新工作階段載入，畫面仍可能在 dashboard 長查詢完成前擷取，故正式站發布後需以延遲載入方式再做行動版驗證。

## 正式站最終驗證

已執行一次真實每日刷新並確認公開 `dashboard.get` payload 含 `TWD=X`、`DX-Y.NYB`、`^IRX`、`^TNX`、`^TYX`；Yahoo 行情抓取加入三次有限退避重試，以降低短暫 429 限流造成的殖利率遺漏。正式公開網站 375px Chromium 實測結果：`window.innerWidth=375`、`document.documentElement.scrollWidth=375`；六個頁籤皆可識別，觀測站可切換，總經卡片、每日摘要生成按鈕、摘要內容與快速提問均可取得。最終截圖顯示美元兌台幣、美元指數、10 年期與 30 年期美債殖利率卡片均已呈現。

## 正式站互動回歸

最新 checkpoint `97461db6` 的 1280px 與 375px 正式站工作階段均在資料載入後實際點擊快速提問，確認收到財經小智回覆；並實際生成每日摘要、確認歷史日期出現，再點擊日期查看摘要內容。兩種視窗均確認總經指標與摘要控制區可讀；375px 的 `scrollWidth` 為 375，桌面版為 1265（視窗寬 1280），均無不預期的水平溢出。

最新正式站腳本直接比較摘要 history API：1280px 與 375px 皆為 `historyChanged=true`，表示生成後歷史回應確實刷新；兩者均找到新日期項目並點擊後看到摘要內容。快速提問、總經卡片及行動版 `scrollWidth=375` 亦再次通過。

正式站 history API 嚴格比對結果：桌面與 375px 的生成前後 `beforeCount=1`、`afterCount=1`，日期陣列均為 `[2026-08-16T00:00:00.000Z]`，但同日期 `generatedAt` 生成後明確較新，`generatedAtUpdated=true`；隨後點擊日期均成功顯示摘要內容，且摘要按鈕恢復可用。
